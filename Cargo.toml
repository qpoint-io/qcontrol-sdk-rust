[package]
name = "qcontrol"
version = "0.1.0"
edition = "2021"
description = "Rust SDK for qcontrol plugins"
license = "MIT"

[lib]
crate-type = ["rlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[build-dependencies]
bindgen = "0.70"
